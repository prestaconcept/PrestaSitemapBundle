framework:
    test: true
    secret: '%env(APP_SECRET)%'
    session:
        handler_id: null
        storage_id: session.storage.mock_file
    php_errors:
        log: true
    router:
        utf8: true
    cache: null
